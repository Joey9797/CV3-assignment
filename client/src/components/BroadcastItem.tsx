import type { Broadcast, BroadcastType } from "@/types/broadcast"

type BroadcastItemProps = {
  item: Broadcast
  type: BroadcastType
}

/**
 * 방송 목록 row (sm 기준 flex 레이아웃 전환)
 */
export function BroadcastItem({ item, type }: BroadcastItemProps) {
  // TODO: implement
  void item
  void type
  return null
}
